# ttt - מדריך הקמה מלא

הפרויקט מורכב משני חלקים. אפליקציית אנדרואיד בתיקייה הזאת, וצד המחשב שקיבלת בקובץ נפרד בשם ttt_pc.zip.

התהליך הוא, קודם בונים את קובץ ה-APK דרך GitHub בלי להתקין כלום במחשב, מתקינים אותו בטלפון, מריצים את צד המחשב, ומחברים.

---

## שלב א, בניית ה-APK דרך GitHub

צעד 1, היכנס ל-GitHub וצור מאגר חדש ריק. לחץ על New repository, תן שם למשל ttt, השאר אותו Private או Public כרצונך, ואל תסמן שום קובץ התחלתי כמו README.

צעד 2, העלה את כל תוכן התיקייה הזאת למאגר. הדרך הפשוטה, בעמוד המאגר החדש לחץ על uploading an existing file, וגרור לחלון את כל הקבצים והתיקיות מתוך תיקיית הפרויקט, כולל התיקייה ‎.github‎ והתיקייה app והקבצים gradlew ו-settings.gradle וכל השאר. לחץ Commit changes.

הערה, אם נוח לך יותר עם שורת פקודה, מתוך תיקיית הפרויקט הרץ את הפקודות הבאות, אחרי שתחליף את הכתובת בכתובת המאגר שלך.

```
git init
git add .
git commit -m "ttt first version"
git branch -M main
git remote add origin https://github.com/YuvalSamson/ttt.git
git push -u origin main
```

צעד 3, ברגע שהקבצים עלו, הבנייה מתחילה אוטומטית. עבור ללשונית Actions במאגר. תראה תהליך בשם Build APK רץ. המתן בערך שלוש עד חמש דקות עד שיופיע וי ירוק.

צעד 4, לחץ על הריצה שהסתיימה, גלול למטה לאזור Artifacts, והורד את ttt-apk. זה קובץ zip, פתח אותו, ובפנים נמצא app-debug.apk.

---

## שלב ב, התקנת האפליקציה בטלפון

צעד 1, העבר את app-debug.apk לטלפון, למשל דרך Google Drive או כבל או WhatsApp לעצמך.

צעד 2, פתח את הקובץ בטלפון. אנדרואיד יבקש אישור להתקנה ממקור לא מוכר, אשר עבור האפליקציה שדרכה אתה פותח את הקובץ, והתקן.

צעד 3, פתח את האפליקציה ttt. אשר את הבקשה להרשאת מיקרופון, ואת הבקשה להרשאת התראות.

---

## שלב ג, הפעלה וחיבור

צעד 1, במחשב, הרץ את צד המחשב לפי ההוראות שבקובץ README_PC.md. החלון יציג את כתובת ה-IP והפורט של המחשב, למשל 192.168.1.20:8765.

צעד 2, בטלפון, הזן באפליקציה את אותה כתובת בדיוק בשדה כתובת המחשב, ולחץ התחבר והפעל. הסטטוס אמור להשתנות למחובר, ממתין ללחיצה במחשב.

צעד 3, במחשב, שים את הסמן בשדה טקסט כלשהו, למשל כאן בצ'אט, ולחץ CTRL+SPACE. דבר בעברית. לחץ CTRL+SPACE שוב כדי לעצור. הטקסט אמור להופיע בשדה.

---

## פתרון תקלות

תקלה, בטלפון כתוב נותק או מתחבר ולא מצליח. בדוק שהטלפון והמחשב על אותה רשת WiFi, ושכתובת ה-IP נכונה, ושאישרת ל-Windows Firewall חיבור נכנס עבור Python ברשת פרטית.

תקלה, לוחץ CTRL+SPACE ולא קורה כלום. ודא שחלון צד המחשב פתוח ורץ. אם הצירוף מתנגש במשהו במחשב, שנה אותו לפי ההוראה שבקובץ README_PC.md.

תקלה, יש צפצוף בכל פעם שמתחיל להקשיב. האפליקציה כבר משתיקה את זה על ידי השתקת ערוץ המדיה בזמן ההקשבה, אבל בחלק מהמכשירים הצפצוף בערוץ אחר. אם זה מפריע, נטפל בזה בגרסה הבאה.

תקלה, האיכות בעברית לא טובה כמו ב-Gboard. תגיד לי, ונבחן מעבר למנוע חלופי או לגישת ה-Accessibility שדיברנו עליה.
